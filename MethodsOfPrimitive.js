let john = {
    name: "John",
    sayHi: function() {
      alert("Hi buddy!");
    }
  };
  
  john.sayHi();

  let str = "Hello";

alert( str.toUpperCase() );

let zero = new Number(0);

if (zero) { 
  alert( "zero is truthy!?!" );
}

